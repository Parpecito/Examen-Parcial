/*import { Handlers } from "$fresh/server.ts";

export const handler: Handlers={
    GET: async(req: Request)=>{
        const formulario= req.url;

        const name= formulario.URLSearchParams.get("name")
        const email=formulario.URLSearchParams.get("email")





    }
}
*/